<%= form_for(lobby_message) do |f| %>
  <% if lobby_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lobby_message.errors.count, "error") %> prohibited this lobby_message from being saved:</h2>

      <ul>
      <% lobby_message.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :message %>
    <%= f.text_area :message %>
  </div>

  <div class="field">
    <%= f.label :lobby_id %>
    <%= f.number_field :lobby_id %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
